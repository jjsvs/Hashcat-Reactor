#pragma once

// Crack feed - a scrollable list of the most recently recovered plaintexts
// with the wall-clock time each one landed. Pushed from the RECOVERED card.
void crack_feed_window_push(void);
